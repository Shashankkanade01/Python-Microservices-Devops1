terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  region = var.region
}

# Optional: get default VPC ID
data "aws_vpc" "default" {
  default = true
}
